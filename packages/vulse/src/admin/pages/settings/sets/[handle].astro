---
import AdminShell from '../../../components/AdminShell.astro'
import SetEditor from '../../../components/SetEditor.vue'

const { handle } = Astro.params
---
<AdminShell title={handle ? `Set: ${handle}` : 'New set'} activePath="/admin/settings/sets">
  <SetEditor client:load handle={handle ?? null} />
</AdminShell>
